import { Typography } from '@mui/material';
import React from 'react';

const RightSection = () => {
    return (
        <div>
            <Typography>text right side </Typography>
        </div>
    );
};

export default RightSection;